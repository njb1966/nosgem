# nosgem

A DOS-era networking and TLS sandbox for experiments and tooling.

## Layout
- `src/` source files and project configuration
- `lib/` third-party libraries and vendor sources
- `obj/` build artifacts (ignored)

## Notes
This repository includes large vendor trees and binary assets. Keep commits focused on project changes.

## Build
Build instructions will be added once the build flow is finalized.
